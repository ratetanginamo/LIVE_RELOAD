// live_perf.js (skeleton)
module.exports = {
  init: ()=>console.log('[LivePerf] initialized'),
  onFileChange: (projectPath, filename) => {
    // Placeholder: real perf metrics require injected client code.
    console.log('[LivePerf] change', filename);
  }
};
